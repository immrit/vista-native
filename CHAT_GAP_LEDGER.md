# دفتر کل شکاف‌های چت (Chat Gap Ledger)
**پروژه:** Vista Native Android (`E:\vista_native`)  
**مرجع فقط-خواندنی قابلیت‌ها:** Vista Flutter (`E:\vista`)  
**مرجع مشاهده‌ای تعاملات:** Telegram UX (`E:\tmp\Telegram`)  
**استاندارد هدف:** Telegram-Level Polish & Architecture  
**تاریخ ممیزی و آخرین بازنگری:** ۱۴۰۵/۰۶/۱۱ (2026-09-01)  
**معمار ارشد:** Senior Android & Jetpack Compose Architect  

---

## ۱. خلاصه مدیریتی و اهداف بازطراحی

هدف این سند، ارائه ممیزی فوق‌دقیق، سخت‌گیرانه و جامع از تمامی شکاف‌های رفتاری، بصری، پایداری Recomposition، هندسه حباب‌ها، سیستم صوتی/چندرسانه‌ای، مدیریت کیبورد و تعاملات لمسی میان پیاده‌سازی مرجع در فلاتر (`E:\vista`) و وضعیت کنونی جت‌پک کامپوز اندروید (`E:\vista_native`) و ارتقای آن به بالاترین سطح استانداردهای تلگرام (Telegram-Level Quality) است.

### اصول بنیادین معماری:
1. **عدم کپی‌برداری از کد/دارایی‌های تلگرام:** طراحی و سورس کاملاً اختصاصی است و تلگرام صرفاً به عنوان استاندارد بنچمارک رفتاری و بصری استفاده می‌شود.
2. **پایداری قرارداد امنیتی E2EE:** تمام قابلیت‌ها بر مبنای رمزنگاری نامتقارن/متقارن محلی (Keystore) و سیاست Fail-Closed عمل می‌کنند؛ هیچ متادیتا یا پیام ناامنی لاگ نخواهد شد.
3. **RTL-First و بومی‌سازی دقیق:** تمامی موقعیت‌ها، ژست‌ها، فونت Vazirmatn، اعداد فارسی و تاریخ‌های شمسی با دقت پیکسلی در حالت راست‌به‌چپ پیاده‌سازی می‌شوند.
4. **تک‌مالکیتی تعاملات (Single State Ownership):** جداسازی قطعی وضعیت‌های `Idle`، `Context` و `Selecting` برای جلوگیری از تداخل ژست‌ها و خطاهای رندری.

---

## ۲. ماتریس جامع ممیزی و ارزیابی شکاف‌ها (Gap Analysis Matrix)

| ردیف | مؤلفه / قابلیت | مرجع Flutter (`E:\vista`) | وضعیت فعلی Native (`E:\vista_native`) | استاندارد هدف تلگرام (Telegram Polish) | وضعیت فنی و اولویت |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **۱** | **چیدمان و لنگر لیست| **۴** | **چیدمان متادیتا و تیک‌های وضعیت** | چیدمان سفارشی `ChatTextBubbleLayout` با محاسبه عرض خط آخر | `ChatTextBubbleLayout` با محاسبه تک‌مسیره (Single-pass) عرض متن و فوتر | قرارگیری متادیتا (ساعت + تیک) در انتهای خط آخر در صورت وجود فضای خالی بدون شکست خط بیهوده (Inline Meta Layout) | ✅ پیاده‌سازی شده و تست‌شده |
| **۵** | **ژست کشیدن برای پاسخ (Swipe-to-Reply)** | کشیدن افقی با بازخورد هپتیک (`HapticFeedback.lightImpact`) | `SwipeToReplyLayout` با `ReplySwipePolicy` جهت‌محور (چپ برای پیام من، راست برای طرف) | مقاومت فنری (Spring Damping)، آشکارسازی روان آیکون پاسخ با Scale/Rotation، لغو نرم در صورت رهاسازی قبل آستانه | 🟡 نیازمند پولیش فیزیک انیمیشن و رفع کامل تداخل با اسکرول عمودی |
| **۶** | **منوی زمینه و ری‌اکشن‌ها (Context & Reactions)** | منوی پاپ‌آپ اختصاصی با نوار ری‌اکشن‌های سریع و بلور | تپ پیام متنی به `MessageContextMenu` با `boundsInWindow` می‌رود؛ long-press مختص Selection است | پس‌زمینه مات (Blurred Scrim)، پاپ‌آپ جهنده به موقعیت حباب، پشتیبانی از کپی، پاسخ، فوروارد، پین، ویرایش، حذف و واکنش‌ها | 🟡 سورس و تست تغییر کرده؛ بازپخش APK جاری لازم است |
| **۷** | **انتخاب چندگانه پیام‌ها (Multi-Selection)** | کنترلر انتخاب با ستون چک‌باکس متحرک | ستون ۴۸dp ثابت در لبه شروع با چک‌باکس متحرک و نوار بالایی عملیات دسته‌جمعی | ورود با Long-press، تاگل با کلیک، همگام با دکمه Back سیستم، عدم تغییر هندسه حباب | ✅ پیاده‌سازی شده |
| **۸** | **کادر ورودی پیام و کیبورد (Composer & IME)** | چندخطی با انیمیشن تغییر ویس/ارسال و پنل ایموجی/استیکر | پنل از footprint واقعی IME رزرو می‌شود؛ درخواست بازگشت به IME پس از commit state پنل در `LaunchedEffect` انجام می‌گیرد | تایپ روان بدون لگ، پشتیبانی از Insets استاندارد با `Modifier.imePadding()`، سوییچ بدون پرش بین کیبورد و پنل رسانه | 🟡 سورس اصلاح شد؛ APK جاری و replay ده‌باره لازم است |
| **۹** | **داک ضبط پیام صوتی (Voice Recording)** | نگه‌داشتن برای ضبط، کشیدن به چپ برای لغو، قفل ضبط | `VoiceRecorderDock` و `VoiceRecordingInteraction` با مدیریت فوکوس صدا | نمایش فرم‌موج زنده (Live Waveform)، تایمر ضبط لحظه‌ای، انیمیشن لرزش و قفل هندزفری با پایداری کامل | 🔄 ارتقا به استانداردهای بصری و پایداری لغو/ارسال |
| **۱۰** | **پخش‌کننده پیام صوتی (Voice Player)** | بابل اختصاصی با سرعت پخش (1x, 1.5x, 2x) و waveform تعاملی | `VoicePlayerBubble` با کنترلر MediaPlayer | نوار پیشرفت قابل جابجایی (Seekable Waveform)، نمایش مدت زمان و سرعت پخش با فیدبک بصری | 🔄 پولیش بصری و همگام‌سازی وضعیت پخش |
| **۱۱** | **رسانه و پیوست‌ها (Media, Video, GIF, Doc)** | نمایش تکی و آلبوم تصاویر، پلیر تمام صفحه و ادیتور مدیا | `AttachmentBubble`، `FullscreenMediaViewerDialog` و `ChatAttachmentBottomSheet` | گوشه‌های متناسب با حباب، لودینگ دایره‌ای پیشرفت دانلود/آپلود روی تصویر، پیش‌نمایش اسناد | 🔄 ارتقای لایوت چندرسانه‌ای و پیشرفت انتقال |
| **۱۲** | **پیش‌نمایش لینک‌ها (Link Previews / OpenGraph)** | کارت‌های غنی وب با تصویر بندانگشتی و خلاصه | استخراج ساده متن لینک بدون کارت غنی | کارت متصل به بابل متن با قابلیت لغو پیش‌نمایش در کامپوزر و کش محلی متادیتا | ❌ پیاده‌سازی نشده / اولویت متوسط |
| **۱۳** | **نقل‌قول و ناوبری ریپلای (Reply Quoting & Jump)** | نوار نقل‌قول رنگی با پرش روان به پیام اصلی | بنر ریپلای متنی ساده در هدر بابل | خط باریک عمودی با رنگ اختصاصی، نام نویسنده اصلی، پرش انیمیشنی به موقعیت پیام و هایلایت موقت | 🔄 نیازمند انیمیشن پرش و هایلایت |
| **۱۴** | **هدایت پیام‌ها (Message Forwarding)** | سربرگ "هدایت‌شده از..." با امکان حفظ یا حذف نام | بنر ساده فوروارد در دیتامدل | سربرگ فوروارد تلگرامی با لینک به پروفایل نویسنده (در صورت مجاز بودن حریم خصوصی) | 🔄 نیازمند ارتقای بصری |
| **۱۵** | **نوار پیام‌های پین‌شده (Pinned Messages Bar)** | بنر چسبان بالای صفحه با سوییچ بین پین‌ها | `PinnedMessagesBar` تعاملی با سوئیچ بین چند پین و لغو پین | نوار باریک زیر اپ‌بار با شمارنده و انیمیشن سوئیچ بین چند پیام پین‌شده | ✅ پیاده‌سازی شده |
| **۱۶** | **سربرگ چت، وضعیت آنلاین و تایپینگ (Header & Presence)** | آواتار، نام، وضعیت آنلاین/آخرین بازدید و ایونت تایپینگ زنده | `TopAppBar` پایه با وضعیت مخاطب | نمایش حالت‌های چندگانه ("در حال نوشتن...", "در حال ضبط صدا...") با اکشن‌های جستجو، تماس و منو | 🔄 پولیش تایپوگرافی و پشتیبانی فونت اسکیل |
| **۱۷** | **نشانگر پیام‌های خوانده‌نشده و دکمه پرش به پایین** | خط جداکننده "پیام‌های جدید" و FAB با بج شمارنده | `UnreadMessagesDivider` + دکمه شناور پرش به پایین با شمارنده | خط جداکننده شیک تلگرامی + دکمه شناور پرش به پایین با شمارنده پیام‌های جدید خوانده نشده | ✅ پیاده‌سازی شده |�ن برای ضبط، کشیدن به چپ برای لغو، قفل ضبط | `VoiceRecorderDock` و `VoiceRecordingInteraction` با مدیریت فوکوس صدا | نمایش فرم‌موج زنده (Live Waveform)، تایمر ضبط لحظه‌ای، انیمیشن لرزش و قفل هندزفری با پایداری کامل | 🔄 ارتقا به استانداردهای بصری و پایداری لغو/ارسال |
| **۱۰** | **پخش‌کننده پیام صوتی (Voice Player)** | بابل اختصاصی با سرعت پخش (1x, 1.5x, 2x) و waveform تعاملی | `VoicePlayerBubble` با کنترلر MediaPlayer | نوار پیشرفت قابل جابجایی (Seekable Waveform)، نمایش مدت زمان و سرعت پخش با فیدبک بصری | 🔄 پولیش بصری و همگام‌سازی وضعیت پخش |
| **۱۱** | **رسانه و پیوست‌ها (Media, Video, GIF, Doc)** | نمایش تکی و آلبوم تصاویر، پلیر تمام صفحه و ادیتور مدیا | `AttachmentBubble`، `FullscreenMediaViewerDialog` و `ChatAttachmentBottomSheet` | گوشه‌های متناسب با حباب، لودینگ دایره‌ای پیشرفت دانلود/آپلود روی تصویر، پیش‌نمایش اسناد | 🔄 ارتقای لایوت چندرسانه‌ای و پیشرفت انتقال |
| **۱۲** | **پیش‌نمایش لینک‌ها (Link Previews / OpenGraph)** | کارت‌های غنی وب با تصویر بندانگشتی و خلاصه | استخراج ساده متن لینک بدون کارت غنی | کارت متصل به بابل متن با قابلیت لغو پیش‌نمایش در کامپوزر و کش محلی متادیتا | ❌ پیاده‌سازی نشده / اولویت متوسط |
| **۱۳** | **نقل‌قول و ناوبری ریپلای (Reply Quoting & Jump)** | نوار نقل‌قول رنگی با پرش روان به پیام اصلی | بنر ریپلای متنی ساده در هدر بابل | خط باریک عمودی با رنگ اختصاصی، نام نویسنده اصلی، پرش انیمیشنی به موقعیت پیام و هایلایت موقت | 🔄 نیازمند انیمیشن پرش و هایلایت |
| **۱۴** | **هدایت پیام‌ها (Message Forwarding)** | سربرگ "هدایت‌شده از..." با امکان حفظ یا حذف نام | بنر ساده فوروارد در دیتامدل | سربرگ فوروارد تلگرامی با لینک به پروفایل نویسنده (در صورت مجاز بودن حریم خصوصی) | 🔄 نیازمند ارتقای بصری |
| **۱۵** | **نوار پیام‌های پین‌شده (Pinned Messages Bar)** | بنر چسبان بالای صفحه با سوییچ بین پین‌ها | استیت پین در مدل پیام، عدم وجود نوار چسبان | نوار باریک زیر اپ‌بار با شمارنده و انیمیشن سوئیچ بین چند پیام پین‌شده | ❌ پیاده‌سازی نشده / اولویت بالا |
| **۱۶** | **سربرگ چت، وضعیت آنلاین و تایپینگ (Header & Presence)** | آواتار، نام، وضعیت آنلاین/آخرین بازدید و ایونت تایپینگ زنده | `TopAppBar` پایه با وضعیت مخاطب | نمایش حالت‌های چندگانه ("در حال نوشتن...", "در حال ضبط صدا...") با اکشن‌های جستجو، تماس و منو | 🔄 پولیش تایپوگرافی و پشتیبانی فونت اسکیل |
| **۱۷** | **نشانگر پیام‌های خوانده‌نشده و دکمه پرش به پایین** | خط جداکننده "پیام‌های جدید" و FAB با بج شمارنده | لنگر دستی در پایین | خط جداکننده شیک تلگرامی + دکمه شناور پرش به پایین با شمارنده پیام‌های جدید خوانده نشده | ❌ پیاده‌سازی نشده / اولویت بالا |
| **۱۸** | **جستجو در پیام‌های گفتگو (In-Chat Search)** | صفحه مجزا و سرچ‌بار اختصاصی با هایلایت کلمات | جستجوی پایه در لیست گفتگوها | سرچ‌بار یکپارچه در بالای صفحه چت با ناوبری نتایج (X از Y) و اسکرول مستقیم به نتیجه | ❌ پیاده‌سازی نشده / اولویت متوسط |
| **۱۹** | **پایداری Recomposition و بودجه فریم (120Hz)** | استفاده از `RenderWindow` و پروفایل عملکردی | کامپوننت‌های بدون `@Immutable` صریح و محاسبه مستقیم در Composition | استفاده از مدل‌های غیرقابل‌تغییر، جلوگیری از ری‌کامپوزیشن‌های اضافه هنگام تایپ و اسکرول سریع ۱۲۰ هرتز | 🔄 بهینه‌سازی مداوم |
| **۲۰** | **معماری آفلاین، صف ارسال و همگام‌سازی (Offline Sync)** | کش Isar با استیت‌های خوش‌بینانه و وب‌سوکت | کش Room با `MessageMergeReducer` و صف آپلود | درج آنی پیام در دیتابیس محلی، ارسال در پس‌زمینه با وضعیت PENDING، تلاش مجدد هوشمند در صورت قطعی شبکه | ✅ پیاده‌سازی شده در لایه Data |
| **۲۱** | **امنیت، رمزنگاری E2EE و چت محرمانه** | رمزنگاری انتها به انتها با پاک‌سازی امن و تایمر حذف خودکار | رمزنگاری متقارن/نامتقارن با Keystore اندروید | سکرت چت با مسدودسازی اسکرین‌شات، تایمر خودتخریبی و پنهان‌سازی پیش‌نمایش | ✅ معماری منطبق |
| **۲۲** | **پشتیبانی متون دوزبانه (BiDi RTL/LTR)** | تشخیص جهت پاراگراف بر اساس کاراکترهای ابتدایی | `TelegramEmojiText`، `ChatComposerField` و `resolveMessageTextDirection` | رندرینگ بدون باگ ترکیب متون فارسی و انگلیسی، فونت Vazirmatn، اعداد فارسی | ✅ پیاده‌سازی شده |
| **۲۳** | **پوسته بصری و والپیپرهای تطبیقی (Wallpaper & Theme)** | والپیپر اختصاصی با پترن‌های تیره و روشن | پس‌زمینه سالید و المان‌های تم متریال ۳ | والپیپر استاندارد با کنتراست داینامیک، شیشه‌ای (Glassmorphism) و سازگار با حالت Dark/Light | 🔄 نیازمند ارتقا به تم اختصاصی ویستا |

---

## ۳. جزئیات دقیق فنی و معماری شکاف‌ها بر اساس لایه‌ها

```mermaid
graph TD
    UI[لایه ارائه Presentation: Compose + StateHolders] --> Domain[لایه دامین: Policies + Models + Mappers]
    Domain --> Data[لایه داده Data: Room DB + WebSocket + UploadPolicy]
    Data --> Security[لایه امنیت Security: Android Keystore + E2EE Cipher]
```

### ۳.۱. لایه Presentation و کامپوزبل‌ها (Presentation Layer)
1. **تفکیک قطعی وضعیت‌های تعاملی (`MessageInteractionState`):**
   - سیستم تک‌مالکیتی تعاملات با سه وضعیت ایزوله: `Idle`، `Context(messageKey, bubbleBounds)` و `Selecting(messageKeys)`.
   - خروج خودکار از حالت انتخاب در صورت خالی شدن لیست کلیدها با تابع `retainSelection(availableKeys)`.
2. **مهندسی لایوت حباب‌های پیام (Inline Metadata Layout):**
   - در تلگرام، متادیتا (زمان + وضعیت تیک) در انتهای آخرین خط متن جای می‌گیرد اگر فضای خالی سمت چپ (در RTL) بیشتر از عرض متادیتا باشد. در صورت کمبود فضا، متادیتا به خط بعدی می‌رود.
   - وضعیت کنونی در Native به صورت دو کامپوننت ستونی یا ردیفی ساده است که در برخی پیام‌های کوتاه باعث پرش یا کشیدگی بیهوده عرض حباب می‌شود.
3. **محاسبات هندسه گوشه‌ها (`MessageBubbleCornerRounding`):**
   - پیام‌های یک گروه متوالی (کمتر از ۱۵ دقیقه فاصله) باید گوشه‌های پیوسته داشته باشند. در پیام‌های من (سمت راست)، گوشه پایین‌راست فقط برای آخرین پیام گرد می‌شود؛ در پیام‌های طرف مقابل (سمت چپ)، گوشه پایین‌چپ فقط برای آخرین پیام گرد می‌شود.

### ۳.۲. لایه تعاملات و ژست‌های لمسی (Motion & Touch Layer)
1. **سیاست ژست پاسخ سریع (`VistaReplySwipePolicy`):**
   - جهت ژست وابسته به سمت حباب است: برای پیام‌های سمت راست (پیام من)، کشیدن به سمت چپ؛ برای پیام‌های سمت چپ (پیام طرف)، کشیدن به سمت راست.
   - آستانه تریگر (Threshold) معادل `64.dp` با ضریب مقاومت کشسانی `0.6f` و ماکزیمم کشش `threshold * 1.7f`.
   - بازخورد هپتیک دقیقاً یک‌بار در لحظه عبور از آستانه اجرا می‌شود.
2. **داک ضبط صدا (`VoiceRecorderDock`):**
   - نگه‌داشتن دکمه میکروفون: ورود به فاز `HOLDING` و شروع کپچر صدا.
   - کشیدن به بالا (`dragOffsetY < -140.dp`): قفل ضبط و ورود به فاز `LOCKED`.
   - کشیدن به چپ (`dragOffsetX < -180.dp`): لغو ضبط و حذف فایل موقت.
   - ضبط با فرمت AAC در کانتینر MP4 با نرخ نمونه‌برداری 44.1kHz و بیت‌ریت 96kbps.

### ۳.۳. لایه جایگذاری پیکسلی منوی زمینه (`MessageContextMenuPlacement`)
- منو و نوار ری‌اکشن‌ها بر مبنای `boundsInWindow` حباب در مختصات پیکسلی پنجره رسم می‌شوند.
- سیستم هوشمند محاسبه حاشیه‌های امن (`safeInsets`) شامل نوار وضعیت، نوار ناوبری و کیبورد (IME).
- اگر بالای بابل فضای کافی برای ری‌اکشن‌ها نباشد، نوار ری‌اکشن‌ها به زیر حباب منتقل می‌شود.

---

## ۴. قراردادهای رفتاری و تعاملاتی استاندارد تلگرام

### جدول ماتریس انتقال حالت‌ها (State Transition Matrix):

| وضعیت جاری | رویداد لمسی / سیستمی | وضعیت بعدی | رفتار بصری و اکشن مورد انتظار |
| :--- | :--- | :--- | :--- |
| **Idle** | نگه‌داشتن انگشت (Long-Press) روی بابل | `Selecting([key])` | بازخورد هپتیک، نمایش نوار بالایی انتخاب و فعال شدن ستون چک‌باکس ثابت در لبه شروع |
| **Idle** | تپ ساده (Click) روی بابل متنی | `Context(messageKey, bounds)` | نمایش پس‌زمینه تار (Scrim)، پرش منوی عملیات و نوار واکنش‌های مجاز متصل به حباب |
| **Idle** | تپ روی لینک، رسانه یا دکمه دانلود | `Idle` (Content Action) | اجرای مستقیم اکشن مربوطه (باز کردن لینک، ویوئر تمام‌صفحه مدیا، شروع دانلود) |
| **Idle** | کشیدن افقی پیام به سمت مخالف حباب | `Idle` (Reply Triggered) | اعمال انیمیشن فنری، فعال شدن بنر ریپلای در بالای کامپوزر پس از رهاسازی |
| **Context** | تپ بیرون از منو / دکمه Back | `Idle` | بسته شدن منو با انیمیشن ناپدیدسازی سریع (Fade-out) |
| **Context** | انتخاب یک اکشن (کپی، پاسخ، ...) | `Idle` (Action Executed) | اجرای اکشن و بسته شدن بلافاصله منوی کانتکست |
| **Selecting** | تپ روی هر پیام | `Selecting(keys')` یا `Idle` | اضافه/حذف کلید پیام؛ در صورت خالی شدن آرایه، خروج آنی به وضعیت `Idle` |
| **Selecting** | دکمه Back سیستم یا دکمه بستن | `Idle` | لغو کامل انتخاب‌ها و پاک‌سازی نوار عملیات دسته‌جمعی |
| **Any** | حذف پیام جاری از طریق ریل‌تایم | وضعیت تصحیح‌شده | بررسی از طریق `retainSelection` یا بستن منوی کانتکست در صورت نبود کلید |

---

## ۵. برنامه عملیاتی گام‌به‌گام و اولویت‌بندی شده (Phased Actionable Plan)

### فاز ۰: همگام‌سازی ابزارهای بیلد و محیط توسعه (Build & Toolchain Baseline)
- [x] جداسازی خروجی بیلد با فایل اینیت `gradle/chat-validation-build.init.gradle`.
- [x] تنظیم صریح `ignored-key`های PGP برای وابستگی‌های تایید شده در `gradle/verification-metadata.xml`.
- [ ] رفع مانع Transport داخلی جاوا (`Selector.open()` و `UnixDomainSockets.connect0`) در ارتباط با Gradle Daemon.
- [ ] کامپایل موفق پکیج تست‌های اینسترومنتیشن چت (`:feature:chat:compileDebugAndroidTestKotlin`).

### فاز ۱: بازطراحی سیستم حباب‌ها و استایلینگ بصری (Telegram Bubble Polish)
- [x] پیاده‌سازی محاسبه انحنای گوشه‌ها در `MessageGrouping.kt` با تست‌های واحد کامل.
- [x] تفکیک فیزیکی بابل‌ها (سمت راست برای پیام من، سمت چپ برای مخاطب مستقل از جهت صفحه).
- [ ] پیاده‌سازی لایوت سفارشی `ChatTextBubbleLayout` در کامپوز جهت ترکیب خطی متن و متادیتا (ساعت + تیک وضعیت).
- [ ] پیاده‌سازی نشانگر وضعیت پیام‌ها (`PENDING`, `SENT`, `DELIVERED`, `READ`, `FAILED`) با آیکون‌های استاندارد تلگرامی.
- [ ] پیاده‌سازی خط جداکننده تاریخ چسبان/شناور (Sticky Floating Date Header) با تقویم شمسی.

### فاز ۲: تعاملات و ژست‌های لمسی (Telegram Motion & Interactions)
- [x] پیاده‌سازی `ReplySwipePolicy` با تفکیک جهت‌محور راست و چپ.
- [x] اتصال `ReplySwipePolicy` به `SwipeToReplyLayout` با مقاومت فنری و انیمیشن بازگشت.
- [ ] تست و تایید عدم تداخل ژست افقی کشیدن برای پاسخ با اسکرول عمودی لیست پیام‌ها در دیوایس واقعی.
- [ ] افزودن انیمیشن آشکارسازی آیکون ریپلای با چرخش و مقیاس (Scale & Rotate).

### فاز ۳: منوی زمینه، ری‌اکشن‌ها و موتور جایگذاری (Context Menu & Placement Engine)
- [x] پیاده‌سازی موتور خالص ریاضی `calculateMessageContextMenuPlacement` برای محاسبه مختصات منو و ری‌اکشن‌ها.
- [x] پیاده‌سازی سیاست دسترسی اکشن‌های منو (`MessageContextActionPolicy`) بر اساس وضعیت پیام و ترنسفر.
- [x] اعمال رنگ‌بندی داینامیک و سازگار با تم تیره/روشن در منوی کانتکست.
- [ ] اتصال انیمیشن بلور پس‌زمینه (Blurred Scrim) هنگام باز شدن منوی زمینه.
- [ ] تایید بازپخش زنده منو در حالت‌های چرخش صفحه (Rotation) و باز بودن کیبورد (IME).

### فاز ۴: بهینه‌سازی کادر پیام و کیبورد (Composer & IME Transitions)
- [x] پیاده‌سازی `ChatComposerField` با ساپورت دوجهته و حذف کاراکتر گرانولار (Grapheme-aware backspace).
- [x] رزرو فضای پنل ایموجی از ارتفاع واقعی کیبورد برای جلوگیری از پرش بصری.
- [x] انتقال به‌روزرسانی ارتفاع اینست‌ها به `LaunchedEffect` کلیددار.
- [ ] اجرای تست سوئیچ ده‌باره کیبورد و ایموجی روی بیلد جدید و ثبت متریک‌های بدون پرش (Zero-jank).

### فاز ۵: سیستم‌های صوتی و چندرسانه‌ای (Voice & Media Systems)
- [x] پیاده‌سازی `VoiceRecorderDock` با ساپورت نگه‌داشتن، لغو کشویی و قفل عمودی.
- [x] مدیریت انحصاری فوکوس صوتی (`AudioManager.AUDIOFOCUS_GAIN_TRANSIENT_EXCLUSIVE`).
- [ ] پیاده‌سازی فرم‌موج تعاملی (Seekable Waveform) در `VoicePlayerBubble` با قابلیت تنظیم سرعت پخش (1x, 1.5x, 2x).
- [ ] بهینه‌سازی پیش‌نمایش آلبوم‌های چندرسانه‌ای و پروگرس آپلود/دانلود دایره‌ای روی تصویر.

### فاز ۶: قابلیت‌های پیشرفته گفتگویی (Advanced Chat Features)
- [ ] پیاده‌سازی نوار پیام‌های پین‌شده (Pinned Message Bar) با انیمیشن جابجایی بین پین‌ها.
- [ ] پیاده‌سازی خط نشانگر پیام‌های خوانده‌نشده (Unread Messages Divider).
- [ ] پیاده‌سازی دکمه شناور پرش به پایین لیست (Scroll-to-bottom FAB) با بج شمارنده پیام‌های جدید.
- [ ] پیاده‌سازی جستجوی درون چت با هایلایت عبارت جستجو شده.

### فاز ۷: اعتبارسنجی عملی، تست‌ها و بیلد نهایی
- [x] اجرای موفق ۱۰۱ تست واحد در ماژول چت (`:feature:chat:testDebugUnitTest`).
- [ ] اجرای تست‌های اینسترومنتیشن کامل روی شبیه‌ساز (`:feature:chat:connectedDebugAndroidTest`).
- [ ] ثبت تصاویر و ویدیوهای تاییدی از تمام سناریوهای تعاملی و ثبت در پوشه گزارشات.

---

## ۶. وضعیت اجرای سخت‌گیرانه و گزارش اعتبارسنجی (Runtime Evidence)

### ۶.۱. موارد تایید شده (Verified Milestones)
- **محدودسازی تغییرات به Native:** تغییرات ناخواسته قبلی در فلاتر لغو شد و مرز توسعه کاملاً در `E:\vista_native` تثبیت شد.
- **سیاست قطعی اکشن‌های منوی زمینه:** پیوست‌های ناقص (`QUEUED`, `UPLOADING`, `FAILED`, `CANCELLED`) تا قبل از `TransferState.COMPLETE` دسترسی ریموت ندارند؛ تست واحد `MessageContextActionPolicyTest` این رفتار را تضمین می‌کند.
- **تفکیک فیزیکی حباب‌ها و ستون انتخاب:** تست اینسترومنتیشن در شبیه‌ساز تایید کرد که حباب‌های فرستنده سمت راست و حباب‌های مخاطب سمت چپ قرار دارند و ستون چک‌باکس انتخاب در یک ریل ثابت ۴۸dp قرار گرفته است.
- **پایداری استیت در ری‌کامپوزیشن:** حذف کالبک‌های اضافه `onGloballyPositioned` از حباب‌ها و تثبیت استیت فیلد کامپوزر با `rememberUpdatedState`.

### ۶.۲. موارد نیازمند بازپخش و تکمیل (Partial / Pending Replay)
- سوییچ ده‌باره ایموجی و کیبورد با حفظ موقعیت ثابت کامپوزر (`y=1298px`).
- بررسی بصری در اسکیل فونت ۲۰۰٪ (`font_scale=2.0`)؛ اپ‌بار برای مقادیر بزرگتر از ۱.۳ به `72dp` ارتقا یافته است اما بازپخش تصویری نیاز دارد.
- اعتبارسنجی تست‌های اینسترومنتیشن روی دیوایس فیزیکی.

### ۶.۳. موانع محیطی و راهکارهای شناسایی شده (Blockers & Root Cause Analysis)
1. **مانع Gradle Daemon Loopback:** در محیط ویندوز کنونی، فراخوانی استاندارد دیمن گرادل به دلیل مسدودی سوکت محلی (`Selector.open()` و `UnixDomainSockets.connect0`) متوقف می‌شود. این مشکل مربوط به پیکربندی شبکه سیستم‌عامل/JDK است و خطای کد کاتلین یا دیتابیس نیست.
2. **پیکربندی وابستگی‌های PGP:** کلیدهای ضروری در `gradle/verification-metadata.xml` به صورت ایزوله ثبت شدند و راستی‌آزمایی SHA-256 با موفقیت انجام می‌شود.
3. **همگام‌سازی مسیر بیلد Hilt:** حل مشکل تداخل دایرکتوری‌های بیلد قدیمی با تنظیم همزمان `layout.buildDirectory` و `buildDir` در اسکریپت‌های گرادل.

---
*دفتر کل شکاف‌های چت - ویرایش ۳.۰ - مستند رسمی مهندسی Vista Native*
